# Status evaluado — bend-spike (2026-09-21)

Revisión directa del código (no solo de los informes de los seis auditores), con reproducción de lo
reproducible. Complementa `PLAN_MEJORAS_2026-09-21.md` (diseños) y `mejoras-2026-09-21/` (secciones).
Entorno: Bend **2.0.24** vía `~/.claude/skills/bend2/scripts/bend_portable.sh` (el pin del repo estaba en 2.0.6;
desde el bloqueante 3 `env/bend.sh` pinea `e52cda4` = 2.0.24 con fallo duro). Bloqueantes 1–5 de §4 ejecutados el
mismo día, en cinco commits; cada ítem de §4 lleva sus números medidos.

## 1. Lo que está bien (verificado)

| Hecho | Evidencia |
|---|---|
| `traces_safe` es un teorema por inducción sobre la traza, universal en orden, configuración y contadores | `v3/PROOF_JETPROT.bend:997` (`run_safe` + `step_safe` derivado del certificado) |
| Los 19 `PROOF*.bend` pasan en 2.0.24; `v3/PROOF_JETPROT.bend` tarda ~153 s en el checker JS | corrida 2026-09-21 |
| Enumeración correcta: 4·4·2·3·4·7 = 2 688 estados | `v3/enum_jetprot.bend:132-154` |
| A-6 declara que no se modela la compensación de R-9 (F2 es límite declarado, no error oculto) | `v3/docs/fase3-fuente.md:234` |
| Volumen: 4 925 líneas Bend en v3, 181 leyes; v2 aporta 65 leyes reutilizables | `wc`/`grep` |

## 2. Hallazgos confirmados en código (por gravedad)

1. **Spec y modelo no separados.** Las 64 leyes son `S.pr_pN(...) == True{}`; cada `pr_pN` es un predicado Bool
   en `v3/jetprot.bend` (líneas ~1096-1140). `unit_eq` es `Nat.is_eq(unit_rank(a), unit_rank(b))` (línea 328);
   `fin_eq` (331) la compone. **No existe** ningún lema `unit_eq(a,b)==True → a==b` (0 usos en LAWS/PROOF).
   Un error en `pr_p9`/`unit_eq` haría vacuas las leyes de marco sin que Bend lo detecte; la única red es C5
   (Python, mismo autor).
2. **Banco de mutantes autorreferencial.** Reproducido: `HB_MAX=4` y `ACK_MAX=3` sobreviven a
   `recheck.catching_laws()` (7 s). Causa: `c6` (`v3/recheck.py:279`) restaura solo 13 funciones y las leyes se
   evalúan con las constantes del modelo mutado (`jetprot_ref.py:293` usa `HB_MAX`/`ACK_MAX` del módulo).
   El "61/62" del README mide una clase de defectos, no las constantes de spec.
3. **F1 — JTT contamina la fila de la tabla.** `jtt_phase` fija `Termination` (`jetprot.bend:463-474`) y
   `concretize` indexa `table(i, p, t)` con esa fase (881). Tras JTT en Heating2, un Slow lee la fila Termination
   → PTN. Sin A-n que lo declare. [S2] describe este problema ("two views of time").
4. **F3 — CommFault ⇒ PTN incondicional** (`jetprot.bend:648`, `dms_on_commfault() = False`). [S1] dice
   "*can* trigger" y condiciona por máscaras de Level-1; [S6] documenta disrupciones perdidas exactamente ahí.
5. **F2 — protección local inhibe la unidad entera por el pulso** (`local_fin`, 499-504; P8). R-9 habla de un
   PINI. Declarado en A-6 como límite de granularidad, pero en dirección opuesta a la fuente: debe ir en el
   resumen, no en un anexo.
6. **Reproducibilidad.** `v3/run.py` sin `argparse` (`--help` lanza el gate de 6 min); Hypothesis con
   `database=None` y sin `derandomize`/seed; `results.json`/`recheck.json` en `.gitignore`; `env/bend.sh`
   clona sin `git checkout <commit>`; `env/check_env.sh:17` usa `bend --version` (eliminado en 2.0.17: es
   `bend version`); sin `requirements.txt`, `CITATION.cff`, CI ni tag.
7. **Deuda documental.** Nombres de leyes distintos entre docs y `.bend` (`stop_honoured` en trazabilidad vs
   `d1_stop_honoured` en LAWS; 32/64 según el auditor); `LAWS_JETPROT.bend:18` dice "2 x 80 640 cells" y son
   104 832 (comentario de una versión con 30 columnas); `step_c_is_the_concrete_step` cierra con `{==}`
   (definicional, no presentarla como ley probada).
8. **Sin liveness.** Ningún teorema de respuesta sobre trazas ("sin heartbeat `hb_max` ticks ⇒ PTN",
   "armado ⇒ Fired en ≤ `ack_max`"). Es lo primero que pide un revisor de seguridad. **→ §4.5: los dos teoremas
   están probados** (`LAWS_JETPROT_LIVE.bend`), universales en `hb_max`/`ack_max` y en la configuración.

## 3. Matices respecto de los auditores

- "TLA+ lo haría en una tarde": cierto para este tamaño, pero TLC no da la universalidad sobre `Nat` de los
  contadores ni sobre el orden como parámetro. El punto válido es la ausencia de liveness, que TLC da gratis.
- "Autor único": el riesgo real es que **todas las capas de evidencia comparten origen** (modelo Bend, `pr_pN`,
  Python ref, mutantes, Hypothesis). Salida: cross-check con herramienta ajena (nuXmv, 1-2 leyes) + lectura externa.

## 4. Bloqueantes para publicar, en orden

1. ~~Lemas `unit_eq_sound`, `fin_eq_sound`, `lvl_le_sound`, `phase_eq_sound`, `dms_eq_sound`, `beq_sound`
   (pruebas por casos) y mover los `pr_pN` a un archivo `spec_jetprot.bend` separado del modelo.~~
   **Hecho 2026-09-21.** `v3/spec_jetprot.bend` (584 líneas, 100 defs: los 9 predicados de igualdad/orden por rango,
   `local_frame`, todos los `pr_p*`/`pr_d*`/`pr_e*` y sus helpers) importa el modelo; `v3/jetprot.bend` queda en
   977 líneas y no importa nada de la spec; leyes, pruebas, certificado (`enum_jetprot.bend`) y tests importan ambos
   (`Spec.` para los predicados, 49+98+205+52 referencias renombradas por script). `v3/LAWS_JETPROT_SOUND.bend`
   (8 leyes) + `v3/PROOF_JETPROT_SOUND.bend` (141 brazos, 0,25 s, sin `?TODO`/`@unsafe`): `beq_sound`,
   `unit_eq_sound`, `dms_eq_sound`, `lvl_eq_sound`, `phase_eq_sound`, `fin_eq_sound` (forma
   `f_eq(a,b) == True{} → a == b`, refutación de los brazos fuera de la diagonal por `clash_ft`), `lvl_le_refl` y
   `lvl_le_sound` (antisimetría bajo los dos órdenes: `lvl_le` es un orden, no una igualdad, así que la forma
   `f_eq` no aplica; se agregó `lvl_eq_sound`, que `fin_eq_sound` necesita y que `LAWS_JETPROT_CONF` usa 98 veces).
   Gate: `PROOF_JETPROT` 163 s (antes 153), `PROOF_JETPROT_CONF`, `PROOF_JETPROT_SOUND` y los 12 PROOF completos
   de `v2/`, `heat/`, `env/` dan `All terms check.`; `FIN`/`FIN_ALT`/`COR` (y `SEQ_A`/`SEQ_B`/`SEQ3_FIN` en v2)
   son parciales que solo reportan TODOs hasta que los importa su PROOF padre, como antes; 10/10 negativos
   rechazados con contraejemplo; smoke 6 `ok`. Diferencias con el diseño (§1 de 02-formal): hipótesis `for h:`
   plana (`+h` solo en `fin_eq_sound`), no `-h`, siguiendo la convención de `LAWS_JETPROT.bend`; los lemas van
   en archivo propio, no dentro de `LAWS_JETPROT.bend`; `run.py` suma `PROOF_JETPROT_SOUND.bend` al gate.
   Pendiente no bloqueante: derivar con estos lemas los corolarios proposicionales de P9/P21/D15–D18/E4.
2. ~~Oráculo de mutantes no autorreferencial (`pymodel/spec_consts.py`, `ORIG` extendido, 11 mutantes nuevos);
   re-publicar el score real.~~
   **Hecho 2026-09-21.** `v3/pymodel/spec_consts.py` (PHASES, RANK, HB_MAX/ACK_MAX, INIT, HEAT_WIN, DMS_TRIG/DMS_WINDOW,
   TABLE, ORD_OF_INSTANCE, transcritos de R-n/A-n) lo importan solo `v3/pymodel/jetprot_laws.py` (nuevo: invariantes,
   las 49 leyes de celda, corolarios, C1/C3/V1 y `check_cell`, movidos desde `jetprot_ref.py` por script, con `C.` para
   toda constante y `R.` para toda función del modelo) y `recheck.py`; `jetprot_ref.py` queda como modelo y no lo
   importa (`grep spec_consts v3/pymodel/jetprot_ref.py` solo da el docstring). `recheck.c6` toma `ORIG` de todo símbolo
   público de `R` y restaura con `vars(R).update(ORIG)`. Dos chequeos nuevos en `catching_laws`: `S0_init_is_spec`
   (INIT del modelo == INIT de la spec) y `S1_inv_all_is_spec` (invariante del modelo == invariante de la spec en
   2688×5×4 estados con contadores), este último al final para que un mutante que atrapa una ley se atribuya a la ley.
   Banco: 62 + 11 (M63–M73) = 73. **Score real: 72/73** (36/37 + 25/25 + 11/11); el único sobreviviente sigue siendo
   M06, equivalente (0 celdas de diferencia); flags 17/17. Cómo caen los 11 nuevos: `HB_MAX=4` y `ACK_MAX=3` por
   `traces_safe_concrete` (y `pres_i6`/`pres_i5` + S1 en modo completo); los dos INIT mutados por S0; `heat_win ∋ Xpoint`
   por E4; `DMS_WINDOW − Termination` por C1 (no por D9 como decía el diseño: D9 es ley del alfabeto abstracto); empate
   JTT/RTPS en el orden 2 por D1; `step_c` que ignora HeatAck en Termination por C3 (ídem, no D9); `reset_ok` con guarda
   extra por D10; `inv_all` del modelo con `<=` **solo** por S1 (es el punto del mutante); una celda de la matriz
   cambiada por C1. Sobre el número anterior: el 61/62 medía 62 mutantes que solo parchaban funciones contra un oráculo
   que leía las constantes del modelo; con el oráculo sobre `spec_consts` esos 62 dan el mismo 61/62, y los mutantes de
   constantes/oráculo que antes sobrevivían (5/6 reproducidos: `HB_MAX=4`, `ACK_MAX=3`, dos INIT, `heat_win`) ahora
   mueren; el 72/73 es el número a publicar, con la salvedad de que M65/M66/M72 caen por comparación directa con la
   spec (S0/S1) y no por una ley de paso. Costos: el sweep completo de leyes tarda ~7 s por mutante; `catching_laws`
   corre V1 y C3 (4,5 s) después de las celdas; las funciones de conformidad devuelven a lo sumo 20 celdas malas;
   `reachable_concrete` se acota en 32 ticks de contador (M32, `t_ack` cuenta con el DMS idle, la hacía infinita en modo
   completo y la corrida llegó a 10 GB antes del tope). `recheck.py --full` / `run.py mutants --full` agregan
   `laws_per_kill` (cuántas leyes atrapan cada mutante). Corrida de referencia (`results.json`, `recheck.C6`):
   histograma leyes→mutantes {1: 37, 2: 15, 3: 8, 4: 6, 5: 3, 6: 2, 12: 1}; **37 de 72 caen por una sola ley**, y son
   puntos débiles declarados: los 10 de la capa concreta solo los ve C1 o C3 (M17–M20, N05–N07, M68, M70, M73), M72
   solo S1, y 26 más de una ley de paso o de demanda cada uno (lista en `killed_by_a_single_law`). Diferencias con el diseño: `succ_phase`
   vive en el modelo y en el oráculo (el mutante N05 lo usa del modelo); `ramp`/`deenergize` del oráculo son propios;
   `reset_ok` en P20/D6/D14 sigue siendo el del modelo, como en las leyes Bend; C1 sigue siendo la única ley que ve la
   matriz (M73 muere solo por ella).
3. ~~Reproducibilidad mínima: pin de Bend con fallo duro, `run.py quick|diff|proofs|mutants|all`, seed +
   `provenance`, `results.json` + `SHA256SUMS` commiteados, `requirements.txt`, `CITATION.cff`.~~
   **Hecho 2026-09-21.** `env/bend.sh` pinea `BEND_COMMIT=e52cda47a58967aa65d1eb26efe8f42a0b0407df` (2.0.24): si el
   checkout falta, baja exactamente ese commit (`git fetch --depth 1` por SHA, 107 MB, 4,3 s medidos desde un directorio
   vacío); si existe en otro commit, se niega a correr (exit 2, mensaje con la salida); `--update` lo lleva al pin,
   `--update <commit>` mueve el pin en el propio script. `env/check_env.sh`: `bend version` (2.0.17 eliminó
   `--version`), commit == pin e `import hypothesis` como chequeos duros, `PY=<launcher>` para Linux/macOS: 13 PASS en
   10 s. `v3/run.py` con `argparse`: `quick` (CONF + SOUND, 1 negativo, smoke, diff guiado 200 trazas sin bug y con 1
   bug, provenance: **9,0 s**), `proofs` (~3 min), `diff` (32 corridas × 3000, 43 s), `recheck` (C5+C2+C3, ~25 s),
   `mutants` (C6; `--full` agrega `laws_per_kill`), `all` (**872 s = 14,5 min con `--full`**, de los cuales C6 591 s y
   `PROOF_JETPROT` 153 s); `--help` imprime la tabla de etapas y tiempos, sin argumentos no lanza nada. Seed:
   `--seed` (20260921) alimenta un `random.Random` por corrida diferencial (`find(..., random=)`); dos corridas de
   `quick` dan las mismas trazas mínimas. Bloque `provenance` (timestamp UTC, versión y commit de Bend, bun, node,
   python, hypothesis, seed, max_examples, SHA-256 de 34 inputs, host) en `results.json` y `recheck.json`. `all`
   escribe además `SHA256SUMS` (36 archivos, formato `sha256sum -c`, verificado). `.gitignore` deja entrar solo
   `v3/results.json` y `v3/recheck.json` (los de `heat/` y `v2/` siguen fuera). `requirements.txt` (hypothesis 6.168.0;
   numpy/jax solo para `heat/`), `CITATION.cff` (URL y autor como placeholders, anotado en `TODO.md`), README (EN/ES) y
   `env/SETUP.md` actualizados a 2.0.24 y a las etapas; `bridge_client.py` respeta `BEND_SRC`. Hallazgo colateral:
   la muestra de ajuste de C2 iteraba un `set` de tuplas de `str`, cuyo orden cambia con el hash seed de cada proceso
   (374/400 y 373/400 en dos corridas con el "seed fijo"); ahora itera la lista ordenada y el número de referencia es
   **371/400 celdas fijan el sucesor (93 %), media 1,13 admisibles de 2688; comandos 195/400, media 1,51 de 9**.
   Gate: `py -3.14 v3/run.py quick` en una copia limpia del árbol (sin `.git`, sin caches): **ok en 8,4 s**, y
   `sha256sum -c SHA256SUMS` verifica ahí. **Incidente del upstream** (2026-09-21 ~19:40 UTC): `github.com/HigherOrderCO/Bend`
   redirige a `github.com/bendlang/bend`, que responde 404 anónimo; la primera prueba de copia limpia con `BEND_SRC`
   vacío disparó diálogos de login del credential manager (un `git fetch` por llamada a `bend.sh`) y hubo que matar los
   procesos. `bend.sh` ahora hace el fetch sin prompts ni credential helper y falla con exit 3 y mensaje (0,7 s), y acepta
   `BEND_BIN=<bend instalado>` como ruta alternativa (rechaza toda versión ≠ `bend 2.0.24`, exit 4). La reproducción "desde
   cero" queda entonces condicionada a conseguir el commit (checkout ajeno o instalador oficial, que sigue en línea);
   ver `env/SETUP.md`. Los cambios de este bloqueante quedaron **sin commitear** a pedido del autor ("continuá sin git");
   mensaje de commit redactado en el scratchpad de la sesión y reproducido en el cierre de la sesión.
4. ~~A-24 para F1 (o fase dual), máscaras por instancia para F3, `Reduced` para F2, `ip_ok` para el DMV.~~
   **Hecho 2026-09-21.** Orden real: Python primero (modelo + oráculo validados sobre todo el dominio nuevo antes de
   tocar Bend), después Bend. **F1**: `Fin{prog, jtt, level, dms, plasma, ip, nb, rf}`; `prog` es la fase de programa
   (indexa la Tabla 1, solo `Advance` la mueve) y `jtt: Bool` dice que el JTT conmutó a la forma de onda de terminación,
   `wave = jtt ? Termination : prog` (leen `wave`: permisivo, ventana del DMV, guarda del fin de pulso, I2/I3). Es el
   mismo modelo que el "`Fin{prog, wave, ...}`" del diseño (el diseño mismo dice que `Advance` mueve ambas en lockstep y
   solo el JTT las separa, siempre hacia Termination) con la mitad de estados y `wave ≥ prog` por construcción (F1c
   igual se prueba). **F2**: `Reduced` (potencia parcial), `local_fin` reduce, `ramp/deenergize` lo tratan como potencia;
   **`Inhibited` se retiró** (A-26): con `Local` reduciendo quedaba inalcanzable y P8 tenía 0 celdas alcanzables (ley
   vacua, lo que C2 rechaza); P8 y E1 desaparecen; **F2d** (`reduced_never_returns`) se agregó porque un `Plasma{True}`
   que devolviera `Reduced → On` pasaba todas las leyes (hallado en la validación Python). **F3**: `CommFault{dms, en}`
   con la máscara como carga del evento (como el orden y la configuración), `Trig.Blind`, `Mask{comm, blind}` por
   instancia, `Inst3` = Tabla 1 con ambos chequeos apagados. **IP**: `ip` es estado (`Ip{ok}`/`XIp{ok}`) y gatea el
   **armado** en el paso abstracto (`arm_dms` exige `ip`), no dentro de `dms_req` como decía el diseño: así IP1 es
   universal. Dominio: 7·2·4·3·2·2·4·4 = **10 752 estados**, 28 variantes, 43 columnas, **462 336 celdas por orden**
   (×4,4); certificado en runtime 27 s (JS) los dos órdenes + corolarios + capa concreta (3 instancias × 7 × 2 × 22 = 924).
   Leyes: 64 → **75** (−P8 −E1 +F1a–F1e, F2a, F2d, F3b, F4, IP1–IP4; P3/P4/P6/P7/P9/P12/P14/P16/P17/P21/D2/D3/D11/D12/E3/E8
   y los dos corolarios de potencia cambiaron de enunciado, marcados "(b4)"); conformidad 117 → **137** (fila `Blind`
   ×14, `dms_trig_Blind`, `inst3_uses_table1`, `mask_inst*` ×3, `blind_masked_no_response`); soundness 8 (regenerada
   para 4 valores de `Heat` y 8 campos). Gates: `PROOF_JETPROT_CONF` (137, 252 filas para `fast_ptn`), `PROOF_JETPROT_SOUND`,
   spec/enum, 10/10 negativos rechazados, smoke 7 `ok`; `PROOF_JETPROT` (escalera nueva: `all_ip`, `all_jtt`, 14 grupos de
   eventos, grupo `g_f`, 112 slices) `All terms check.` en **11 min 23 s** (era 153 s con 104 832 celdas por orden: el
   costo escala ~linealmente con las celdas). F1a y F1c viven en helpers `f1a_go`/`f1c_go` porque Bend no deja hacer
   `match` sobre un campo de un parámetro `+f` de ley dentro del `def Laws.*`; IP2 se deriva de P16 por `implies_e`. Python: `jetprot_ref`/`spec_consts`/`jetprot_laws` (55 leyes,
   índices `P J L D PL IP NB RF`), `prod/jetprot_prod.py` (8 bugs plantados: +`jtt_moves_program_phase`,
   +`arms_below_threshold`, `plasma_ok_clears_inhibit` → `plasma_ok_restores_reduced`), `bridge.mjs`, `recheck.py`
   (3 instancias, `concretize` sobre el estado), `run.py` (`XIp`, `Blind`, 7 líneas de smoke, 3 instancias),
   `mutants.py` convertido por AST (36 tuplas, 27 destructuras, índices remapeados; M05/M22/M31/N08 hablan ahora de
   `Reduced`; M06/N15/N20 respetan el gate de corriente para seguir siendo de un defecto). Banco: **72/73** (36/37 +
   25/25 + 11/11) en 294 s, M06 sigue equivalente (0 de 924 672 celdas). `quick` 32 s (el smoke evalúa el certificado en
   runtime: 27 s de los 32). Docs: A-6/A-7/A-9/A-13/A-21/A-22 reescritas, A-24/A-25/A-26 nuevas, `fase3-fuente.md` §5,
   `fase3-diseno.md` §1/§2/§6/§7/§9 (H33–H35), trazabilidad (SR-9 reescrita, SR-16/17/18 nuevas), seguridad, README §3,
   nota en el borrador del preprint. **La métrica de ajuste hizo su trabajo:** la primera corrida `all` sobre el modelo
   nuevo dio 329/400 (82 %, media 1,48) contra 371/400 antes; el diagnóstico (qué campos variaban entre sucesores
   admisibles) mostró dos huecos: ninguna ley enmarcaba las **unidades** ante `Ip`, `Plasma{True}`, un `Tick` sin
   watchdog o una parada rechazada (antes las fijaban P8 y los 4 valores de `Heat`), y `jtt` quedaba libre cuando
   `prog = Termination` (dos estados bisimilares). Se agregaron **F4** (`units_frame`) y **F1e** (`jtt_exact`), validadas
   en todo el dominio: ajuste **397/400 (99 %), media 1,01**. La misma corrida mostró que `commfault_leaves_heating`
   es **inobservable en la instancia 3** (el chequeo está enmascarado: es F3 en acción); `run.py` lo declara
   (`unobservable_configs`) en vez de exigirlo. Corrida `all` de referencia: `all: ok` en **37 min** (`PROOF_JETPROT` 1064 s con el checker de liveness corriendo en
   paralelo; solo, 11–18 min), C5 924 672 celdas = Python, C2 397/400, C6 72/73, diff 26/27 + 1 inobservable declarada;
   `results.json`, `recheck.json`, `SHA256SUMS` regenerados. **Costo:** el gate completo pasó de 15 a ~37 min y `quick`
   de 9 a 32 s (el smoke evalúa el certificado en runtime); el checker escala linealmente con las celdas.
5. ~~Dos teoremas de liveness acotada sobre trazas.~~ **Hecho 2026-09-21.** `LAWS_JETPROT_LIVE.bend` (2 leyes +
   `ticks`, `no_hb`, `no_reset`): **`watchdog_responds`** (desde cualquier estado que cumple `inv_all`, toda traza
   sin `Heartbeat` ni `Reset` con al menos `hb_max` ticks termina con el PTN enclavado) y **`dms_responds`** (desde
   cualquier estado que cumple `inv_all` con el DMS armado, toda traza sin `Reset` con al menos `ack_max` ticks
   termina con el DMS disparado). Universales en el orden, la configuración (viaja en los eventos), los contadores
   y los dos límites: nada del teorema lee el certificado. Prueba: `PROOF_JETPROT_LIVE_CORE.bend` (842 líneas, 12
   leyes auxiliares + 56 defs; inducción sobre la traza con el reclamo reforzado `ptn ∨ hb_max ≤ ticks(resto) + hb`
   y `fired ∨ (armed ∧ ack_max ≤ ticks(resto) + tack)`; el caso `Nil` lo refutan I6/I5 por antisimetría de `Nat`,
   el caso `Con` lo cierra un lema de paso por evento) a partir de **ocho hechos de celda**: `step_safe`
   (preservación) y P1, D4, D7, D8, P15, P18, E11. **Diferencia con el diseño (`02-formal.md` §2):** los hechos no
   pueden ser leyes abiertas en el core (Bend 2.0.24 rechaza código vivo que llame a una ley sin llenar: "an
   unfilled law is a dead claim"); son **hipótesis template** (`for ~fact: @+o -> ... -> {...}`) de los dos
   teoremas cerrados `watchdog_from`/`dms_from`, y `PROOF_JETPROT_LIVE.bend` (32 líneas) los instancia con las leyes
   de `LAWS_JETPROT.bend` tal como las prueba `PROOF_JETPROT.bend` por reflexión. El diseño usaba E6/D6/E8; la
   prueba real necesitó D4+P1 (watchdog) y D7+D8+P15+P18+E11 (DMS), y las hipótesis van con `+` (se usan dos
   veces), no erradas. Gates: `bend PROOF_JETPROT_LIVE_CORE.bend` → exactamente `Error: 2 TODOs found.` (los dos
   `Laws.*` quedan abiertos ahí a propósito; 1 s, sin certificado); `bend PROOF_JETPROT_LIVE.bend` →
   `All terms check.` en **1098 s** (18.3 min) (re-chequea `PROOF_JETPROT` y los certificados: es el gate que reemplaza a
   `PROOF_JETPROT.bend` en `run.py`, `PROOFS = [LIVE, CONF, SOUND]`). Corrida `all --full` de referencia: `all: ok` en **94 min** (todo verde; `PROOF_JETPROT_LIVE` 1098 s, `PROOF_JETPROT_CONF` 0.6 s, `PROOF_JETPROT_SOUND` 0.4 s), C5 924 672 celdas = Python, 0 discrepancias, C2 397/400 (99 %), C6 72/73, diff 26/27 + 1 inobservable declarada; C6 `--full` 63 min de los 94, el resto como en §4.4
   (`results.json`, `recheck.json`, `SHA256SUMS` regenerados; las tres LIVE entran en la procedencia). Lo que sigue
   sin probarse (A-15): que el pulso termine, y cualquier cota en milisegundos: la cota es en ticks abstractos.
6. Cross-check externo de una ley; nombres de leyes doc↔bend con script; vocabulario fijo
   ("decidido por cómputo y elevado por reflexión").

Veredicto: el método sobrevive la revisión; el caso necesita ~2 semanas antes de mostrarse afuera.
