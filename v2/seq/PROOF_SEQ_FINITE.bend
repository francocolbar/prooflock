# PROOF_SEQ_FINITE.bend - the finite certificate is decided by the checker's evaluator.
import Base
import ./seq.bend as S
import ./seq_enum.bend as E
import ./LAWS_SEQ_FINITE.bend as Laws

def Laws.finite_check():
  {==}
