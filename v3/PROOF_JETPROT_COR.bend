# PROOF_JETPROT_COR.bend - the state corollaries under inv_fin, by computation (2688 states).
import Base
import ./jetprot.bend as S
import ./enum_jetprot.bend as E
import ./LAWS_JETPROT.bend as Laws

def Laws.corollaries_check():
  {==}
