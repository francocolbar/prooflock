# Phase 3 — Source: the JET wall-protection chain (RTPS + PTN)

Date: 2026-09-19, **revision 3** (after two rounds of adversarial review; record in `phase3-design.md` §9); **revision 4, 2026-09-22**: register checked against the code, against the new sources [N1], [N2], [K15], [M13] (§2) and against the physics library (evaluation in `SUPUESTOS_EVALUACION.md`, Spanish): A-3, A-5, A-16, A-22 and A-24 rewritten, the direction labels of A-1, A-2, A-6, A-7, A-8, A-9, A-11, A-12, A-16 and A-24 corrected, A-27…A-35 added. Two changes came with it: the parts of P1 and D1 that concern the PTN and the primary stop are now named laws, apart from our soft-over-soft policy (`p1a_ptn_latched` and `d1b_primary_honoured` restate JET sources; `d1a_ptn_honoured`, `piw_after_ptn_is_noop` and `p1b_stop_never_cleared` join a JET statement with our formalisation: the basis of each is in A-2); and the concrete layer has a per-instance secondary stop response (A-3), which keeps the previous reading in instances 1–3 and uses an illustrative PTN-preferred table in a fourth instance (A-35). Updated 2026-09-23: `p1a_ptn_latched` rests on R-0 as well as [N1] (A-2); the "If false" cells of A-9, A-11 and A-12 list the laws the catalogues attribute to them; §5 no longer gives a cause for the old 22-event count; the framing paragraph follows Fig. 1 and R-0 of [S1], the note on the MHD = None column follows [N1], and the ditto check is attributed to automated reviews; then R-15 quoted on to the cause of its 7 late detections, retitled, and the same cause given in A-22. Status:
**pending confirmation**. This document fixes which real system is modelled, what was extracted verbatim from the
publications and what was simplified. Anything the model asserts that is not in the "verbatim" column is a modelling
decision of ours and is declared as such, with its direction of conservatism.

**Framing, stated first.** The RTPS is a **machine protection** system (investment protection): Fig. 1 of [S1] colours
it "SOFT PROTECTION" and the PTN, the CISS and the PEWS "HARD PROTECTION"; R-0 names the CISS, the PTN and the PEWS as
JET's historical machine-protection systems (the CISS "provides basic hardwired plant protection"), and personnel
safety is the PSACS ([S2]). What is modelled is, in the words of [S1], the **Stop Selector** ("implements the state
machine and alarm processing logic"), not the **Stop Manager** ("defines and executes the stop responses"): the
override waveforms to the five actuator systems are out of scope. The evidence this phase produces is about a
*specification* of protection logic, not about a system, and is not evidence of a nuclear safety function.
The argument that the *method* transfers to category A/B/C functions (IEC 61513/61226) is made separately
(`phase3-safety.md`).

## 1. Candidates reviewed

| System | What was found | Verdict |
|---|---|---|
| **JET — Real-Time Protection Sequencer (RTPS) + Pulse Termination Network (PTN)** | Functional description published under CC BY: pulse phases, seven stop triggers, three response types, **the phase × trigger → response table**, primary/secondary response, local protection, latching of the PTN output, firing sequence of the disruption mitigation system (turn off heating → acknowledgement or timeout → injection), watchdog, "blind" alarms; and, in [S6], the rule connecting the DMV to the PTN, the DMV enable window and thresholds, the switch-off times and the record of disruptions missed by configuration. | **Chosen.** It is the only one with discrete logic published at table level, in open sources, and with a size that fits in the certificate. |
| ITER — Central Interlock System / Plant Interlock System: A. Vergara Fernández et al., *Modeling tools for the ITER Central Interlock System*, FED 86 (2011) 1137–1140; L. Fernández-Hernando et al. (incl. A. Vergara), *The ITER interlock system*, FED 129 (2018) 104–108; Barrera et al., FED 129 (2018) 73–77 | Architecture (slow PLC / fast FPGA / hardwired), function categories, deployment methodology. The concrete functions and their permissives are in IO internal documents; the FED papers are not CC-licensed and the public abstracts are not enough to extract an events → actions table. The ICALEPCS papers (open) on the CIS were not reviewed in depth. | Discarded for this phase (possible second case). |
| ASDEX Upgrade — Discharge Control System, exception handling: W. Treutterer et al., FED 89 (2014) 146–154, DOI 10.1016/j.fusengdes.2014.01.001 (green copy in MPG PuRe; the publisher's version is not CC) | Describes the *framework*: segments, segment scheduler, local and central event detection. The concrete reaction logic is per-discharge configuration (repair segments "laid down in the pulse schedule"); it does not publish a fixed event → action table. | Discarded: there is no fixed table to model. |
| MAST-U — Real-Time Protection System: S. Hall, P. Jacquet, G. Naylor, FED 146 (2019) 421–425, DOI 10.1016/j.fusengdes.2018.12.082; preprint UKAEA-CCFE-CP(19)29 | Threshold monitoring (currents, axial forces, stored energy, I²t) → actions (stops to sources, controlled stop by the PCS, vertical *kick*); "breaches are mapped to actions", with configurable mappings. The expression "state machine" does not appear in the paper. | Discarded: it is a threshold table, not a sequencer. |
| KSTAR — Fast/Supervisory Interlock | Only abstracts were reviewed (FED, paywall). | Not evaluated in depth; noted. |

## 2. Sources chosen

- **[S1]** A. V. Stephen et al., *Centralised Coordinated Control to Protect the JET ITER-like Wall*, Proc. ICALEPCS
  2011, Grenoble, paper FRAAULT04, pp. 1293–1296, ISSN 2226-0358 (JACoW 2011 assigns no DOI; the citation of record is
  URL + id). CC BY 3.0. Copy in `sources/Stephen2011_ICALEPCS_FRAAULT04_CC-BY-3.0.pdf`, text in
  `sources/Stephen2011_FRAAULT04.txt` (with attribution header). **Main source.**
  https://accelconf.web.cern.ch/icalepcs2011/papers/fraault04.pdf
  (Note: the reference list of [S3] cites this paper as "(2012) 1423"; the correct pages are 1293–1296.)
- **[S2]** J. Waterhouse, M. Wheatley, A. Stephen, C. Hogben, G. Jones, A. Goodyear, T. Farmer, P. McCullen, *JET CODAS —
  the final status*, Fusion Eng. Des. 210 (2025) 114737, DOI 10.1016/j.fusengdes.2024.114737. **CC BY 4.0** (Crown
  Copyright 2024, Elsevier). Verbatim excerpt of §3.4 in `sources/Waterhouse2025_sec3-4_excerpt_CC-BY-4.0.txt`. The
  full PDF is not copied by choice, not by licence restriction. (It writes "Real-Time Protection **Sequence**";
  [S1]/[S3] write "Sequencer".)
- **[S3]** J. S. Edwards et al., *Robust configuration of the JET Real-Time Protection Sequencer*, Fusion Eng. Des. 146
  (2019) 277–280, DOI 10.1016/j.fusengdes.2018.12.045 (reference of record; not CC). Open preprint UKAEA-CCFE-CP(19)30,
  SOFT 2018: https://scientific-publications.ukaea.uk/wp-content/uploads/UKAEA-CCFE-CP1930.PDF. Its "do not
  circulate" legend is conditioned on "prior to publication of the original", which has already occurred; it is cited, not copied.
- **[S4]** D. Alves et al., *The Software and Hardware Architectural Design of the Vessel Thermal Map Real-Time System in
  JET*, Proc. ICALEPCS 2011, WEPMN014, pp. 905–908. CC BY 3.0. Copy in `sources/`. Context for the thermal alarms
  (DHS; 10 ms cycle).
- **[S5]** M. Jouve et al., *Real-time protection of the "ITER-like Wall at JET"*, Proc. ICALEPCS 2011, WEPMU018,
  pp. 1096–1099. CC BY 3.0. Protection cameras; context only. **Acronym clash:** in [S5] "RTPS" is the camera processing system ("The Real Time Processing System (RTPS) has been divided into a", p.1097), not the Real-Time Protection Sequencer: [S5] is not a source on the sequencer.
- **[S6]** C. Reux, M. Lehnen, U. Kruezi, S. Jachmich, P. Card, K. Heinola, E. Joffrin, P. J. Lomas, S. Marsen, G. Matthews,
  V. Riccardo, F. Rimini, P. de Vries and JET EFDA contributors, *Use of the disruption mitigation valve in closed loop for
  routine protection at JET*, Fusion Eng. Des. 88 (2013) 1101–1104, DOI 10.1016/j.fusengdes.2012.12.026. Open preprint
  EFDA–JET–CP(12)05/22 (SOFT 2012): https://scipub.euro-fusion.org/wp-content/uploads/2014/11/EFDC120522.pdf. Cited,
  not copied. **Added in revision 3**: rule connecting the DMV to the PTN, window and thresholds, times, misses.
- **[S7]** C. I. Stuart et al., *PETRA: A generalised real-time event detection platform at JET for disruption prediction,
  avoidance and mitigation*, Fusion Eng. Des. 168 (2021) 112412, DOI 10.1016/j.fusengdes.2021.112412 (open access).
  Post-2018 consolidation of the DMS triggers; enabling conditions of the alarms. The copy consulted (UKAEA repository) states "All rights reserved": cited with short quotes, not copied.
- **[N1]** G. De Tommasi, A. C. Neto, F. Maviglia, P. J. Lomas, P. McCullen, F. G. Rimini and JET EFDA contributors, *Plasma Position and Current Control System Enhancements for the JET ITER-like Wall*, preprint EFDA-JET-PR(13)06, https://scipub.euro-fusion.org/wp-content/uploads/2014/11/EFDP13006.pdf (journal version not checked). §4.1 "The Terminations Management System" and Figs. 6–7: PIW and PTN stops, primary and secondary stops, the time-window rule of the shape controller, the cap of two stops in sequence, the stop statistics. The only open text found that reports how JET's primary and secondary stops were configured and used (counts, preferred secondary); the mechanism itself is in [S1] (R-6). Its legend says it "may not be further circulated": **cited with short quotes, never copied**. Pages are PDF pages of the preprint (printed page = PDF page − 4). **Added in revision 4.**
- **[N2]** A. Neto et al., *Shape Controller Upgrades for the JET ITER-like Wall*, Proc. ICALEPCS 2011, Grenoble, paper MOPMU035, pp. 514–517. CC BY 3.0. https://accelconf.web.cern.ch/icalepcs2011/papers/mopmu035.pdf. The jump to termination on the shape-controller side (p. 516) and the shape controller's own path to PTN/CISS (p. 514). Cited, not copied. **Added in revision 4.**
- **[K15]** U. Kruezi, S. Jachmich, H. R. Koslowski, M. Lehnen, S. Brezinsek, G. Matthews and JET EFDA contributors, *A new Disruption Mitigation System for Deuterium–Tritium Operation at JET*, Fusion Eng. Des. (2015); preprint CCFE-PR(17)36 (UKAEA is the copyright holder). The two valves DMV1/DMV2, the PTN fibre trigger, the "safe state" condition before injection, the times of flight, and the 2 MA / 5 MJ limit for operation without DMS. Pages are PDF pages of the preprint. Cited, not copied. **Added in revision 4.**
- **[M13]** M.-L. Mayoral et al., *On the Challenge of Plasma Heating with the JET Metallic Wall*, arXiv:1309.0948 (journal version in Nucl. Fusion, 2014, not checked). PEWS2 and the LH arc protection (16 arcs seen, 4 not stopped in time, p.17). Cited, not copied. **Added in revision 4.**
- **Physics references** used in §4 (library `../papers/`, index `papers/MANIFEST.md`; journal page numbers): [IPB99-C2], [IPB99-C3], [IPB99-C6], [IPB99-C8] = *ITER Physics Basis*, Nucl. Fusion 39 (1999), chapters 2 (confinement and transport), 3 (MHD stability, operational limits and disruptions), 6 (auxiliary heating and current drive) and 8 (plasma operation and control); [IPB07-C3] Hender et al., [IPB07-C6] Gormezano et al., [IPB07-C8] Gribov et al. = *Progress in the ITER Physics Basis*, Nucl. Fusion 47 (2007), chapters 3, 6 and 8; [Eich13] T. Eich et al., Nucl. Fusion 53 (2013) 093031.

## 3. What was extracted verbatim

Quotes are from [S1] unless indicated. Numbered **R-n**; their class (context / architecture / functional requirement /
configuration datum / justification) is in `phase3-safety.md`, where the safety requirements are derived.

### Architecture of the chain (context)

- **R-0** "JET machine protection has been provided historically by three systems. The Central Interlock and Safety
  System (CISS) provides basic hardwired plant protection. Higher level protection with limited configurability is
  provided by the Pulse Termination Network (PTN). [...] The PTN output is a latched stop signal to each of the control
  systems, which execute a fixed shutdown sequence in response. Finally, additional heating systems require enable
  signals from the Plant Enable Window System (PEWS) which are conditioned by real-time protection signal validation
  algorithms." [S2]: "CISS was built as a finite state machine with combinatorial input logic."
- The RTPS is "a centralised controller which responds to the VTM and WALLS alarms by providing override commands to the
  plasma shape, current, density and heating controllers". 500 Hz cycle; VTM inputs at 100 Hz. Two modules:
  "the Stop Selector GAM which implements the state machine and alarm processing logic, and the Stop Manager GAM which
  defines and executes the stop responses."
- Supervisor states: "A supervisory level set of tasks implements a state machine distinguishing background
  operation, preparation for an experiment (pulse), pulse on, and post-pulse actions".

### Pulse phases

- **R-1** "Pulses are split into several timed 'phases'. Responses can be defined that vary by phase and category of
  alarm for each pulse." [S3]; and "Available states are defined by Level-1 so no hard-coded states are required" [S3].
  The seven phase names (**Breakdown, Ip Rise, Limiter, X-point, Heating 1, Heating 2, Plasma Termination**) are the
  row labels of Table 1 of [S1] (R-5), not prose. [S6] lists another, open, vocabulary: "in which phases of the
  discharge (current ramp-up, X-point formation, heating, scenario termination, ...) the DMV is to be enabled".
  **Conclusion: the set of phases is a configuration instance, not a property of the system.**

### Stop triggers

- **R-2** "Seven stop triggers have been identified. Three represent thermal problems. These are hot spots in the main
  chamber (inner and outer walls), in the divertor [...] or which occur in both regions. Two are associated with
  magneto-hydrodynamic (MHD) instabilities. Two more allow for generic conditions requiring either a slow or fast
  termination of the pulse."

### Stop responses

- **R-3** "There are three kinds of stop response. If a fault occurs early in the discharge before main heating phase,
  it can be sufficient to trigger the PTN system. During the heating phase, the real-time controllers require overrides
  which are fully programmable, termed RTPS stops. In some cases, the best possible way to end a pulse is to execute the
  control waveforms that would have come into force had the experiment reached a natural conclusion. This is termed a
  jump-to-termination (JTT) stop." And on the overrides: "the override is specified as a waveform which is
  parameterised as a set of steps, where for each step, the reference value to achieve and the time in which to make
  the transition are defined [...] If 10MW of neutral-beam total power were being delivered and a stop response was
  initiated, the new reference could be set to 6MW".
- **R-4** [S2]: "RTPS was also able to take control of the waveforms used to drive the shape controller and the heating
  systems such as RF and Neutral Beams via their local manager to ramp down the plasma current and heating power to
  give a softer landing for the plasma termination. [...] On a jump to termination, RTPS would signal to the shape
  controller, heating system local manager and gas injection local managers to move forward in their waveforms to the
  termination region." And the self-criticism: "A better language would have been time within a sequence of plasma control
  and jumps from within one sequence to the start of another, the termination sequence".
- The motivation for the RTPS: "The new system should act early, to prevent PTN or CISS from following a global stop strategy
  that might cause excessive energy loads on the walls."

### The configuration table (Table 1 of [S1])

- **R-5** "Table 1 illustrates the matrix that links stop triggers to stop responses, as a function of experimental
  phase. The full control matrix is a key part of the protection system configuration interface." Caption: "Table 1:
  The **primary** stops table configures the mapping between stop triggers and stop responses as a function of
  experimental phase. A subset of the possible stop triggers are shown, including mode lock (MHD), main chamber hotspot
  (MCHS) and divertor hotspot (DHS)." (The first trigger column, "Slow" (A-31), is identified with the slow-termination trigger of
  R-2, "either a slow or fast termination": our inference.)

  | Phase | Slow | MHD | MCHS | DHS |
  |---|---|---|---|---|
  | Breakdown | PTN | None | None | PTN |
  | Ip Rise | PTN ⁱ | None ⁱ | None ⁱ | PTN ⁱ |
  | Limiter | PTN | None ⁱ | None ⁱ | PTN ⁱ |
  | X-point | PTN | None ⁱ | None ⁱ | PTN ⁱ |
  | Heating 1 | RTPS | None ⁱ | RTPS | PTN |
  | Heating 2 | RTPS | None ⁱ | RTPS | JTT |
  | Plasma Termination | PTN | None ⁱ | PTN | PTN |

  ⁱ = cell read from a "ditto" mark (`|`); **15 printed cells, 13 by ditto**. Forensic verification by two separate
  automated reviews (not independent assessment, README §7): page 1295 contains no vector object (`lines=[]`,
  `rects=[]`, `curves=0`); each `|` is the U+007C glyph in the CMSY10 font centred on the x coordinate of its column
  to < 0.2 pt; in each of the 7 rows, printed cells + glyphs = 4; and where a column carries a printed value
  (`Limiter PTN`) the glyph does not appear. They are "ditto" marks, not table rules.

  It is an **example configuration** ("illustrates"), not the fixed logic of the system: the full matrix is defined by the
  *session leader* per pulse. There is a **secondary** matrix that is not published (A-16). In the model the matrix is
  data; the rule is certified for every matrix and this instance by conformance (`phase3-design.md` P11).

  **The MHD = None column does not mean "mode lock does nothing at JET".** [S6]: the mode lock "is already used at JET
  to soft-stop the pulse, albeit with a lower value than the one used to trigger the DMV"; and "a bad situation
  (mode-lock) has already been detected and soft-stop strategies have been initiated. They aim at reducing the
  disruption forces by transiting to a low-triangularity configuration". [S7]: PETRA "triggers a stop and MGI" on a
  locked mode. The tables JET configured did answer it: in pulses 80450–83974 (the ITER-like wall campaigns of
  2011–2012) the MHD-driven stops, "together with those driven by wall hotspots, account for the vast majority of
  primary PIW stops" ([N1] PDF p.14), and the PIW stops are "triggered by the real-time protection sequencer (RTPS)"
  (PDF p.12). Table 1 "illustrates" one configuration (R-5). The same signal had two thresholds with two responses: a
  soft stop, and the DMV, whose triggering [S6] attaches to a stop sent to the PTN "either directly or through an RTPS
  response". Up to 2018 the detection for disruption mitigation ran in VME systems or in the RTCC ([S7] p.2). "None"
  means "no primary RTPS response in this example", not "no protection". That is why the model certifies a **second
  instance** in which MHD reaches the PTN and arms the DMS (`phase3-design.md` §6), with cells of our own (`inst2_*`,
  labelled assumptions in the catalogues).

### Primary and secondary; hierarchy

- **R-6** "The system also takes account of the possibility that one class of fault (and corresponding stop response)
  might be followed soon after by another. [...] This is addressed, by allowing for two levels of stop response,
  primary, and secondary. Not all possible combinations of control are allowable, since in some cases, once a primary
  stop response has begun, the best outcome is achieved by allowing it to run to completion."
- **R-7** [S2]: "This could initiate slow and fast termination of the plasma through PTN via 16 configurable direct
  connections. Unlike PTN, RTPS was able to act hierarchically so that subsequent alarms could generate a more urgent
  stop." [S6]: "The triggering of the DMV can be attached to any of the stops sent to the Plasma Termination Network
  (PTN) either directly or through an RTPS response."
- Vocabulary note: "slow/fast" names in R-2 two *triggers* and in R-7 two *output classes* of the PTN. The PTN
  is not a single response; in the model it is (A-20).

### Local protection

- **R-8** "Local alarms are limited in location or time, or may be set at lower thresholds and trigger a protection
  response which allows continued operation with dynamically controlled limits on further injection of heating with
  fine granularity. If this fails to bring checks back into tolerance, global alarms are raised which result in control
  overrides which truncate the experiment and land the plasma safely."
- **R-9** "when a local hotspot alarm occurs for such an element, the relevant PINI should be turned off to prevent
  further overheating. This should not preclude the neutral-beam system as a whole from continuing to deliver the total
  requested power to the plasma, as other PINIs can be turned on to compensate."

### PTN: latching and disruption mitigation sequence

- **R-10** [S2]: PTN "consists of a set of inputs from physical plant (inc. CISS) and programmable inputs from software
  implemented protection systems that are mapped onto a set of control outputs which stop various [*sic*] plant systems.
  Additionally, inputs and outputs can be enabled or disabled and inputs can be generated on a timer and latched and time
  stamped on activation." (The latching of the *inputs* is a configurable capability; the unconditional statement
  of latching is that of the *output*, in R-0.)
- **R-11** [S2]: "Several PTN outputs are used to trigger the Disruption Mitigation System (DMS) [...]. As the injection
  of a massive quantity of gas into a plasma with Neutral Beam or RF heating would be undesirable, these triggers first
  cause the heating systems to be turned off and then conditioned with an acknowledgement from the heating plant (and
  timeout) before activating the DMS." [S6] gives the engineering reason and the times: "Heating systems cannot be operating
  when the DMV is activated (limitation beam duct pressure and antennae lines). Consequently, they have to be switched
  off via interlocks before the triggering of the valve. [...] It takes only 2 ms to switch off the NBI power supplies,
  but approximately 38 ms to switch off RF. Due to the fact that no feedback signal is sent by RF plant to confirm that
  power supplies have been switched off, additional margin was taken leading to an overall delay of 50 ms between the
  request and the actual injection." It is protection of the *heating plant*, with **timeout as the real path**
  (RF does not acknowledge).
- **R-14** [S6], DMV enabling: "The DMV was used systematically for scenarios above 2.5 MA, and for some other
  risky scenarios between 2.0 MA and 2.5 MA. In addition, it is usually left active down to 1.75 MA. The use of the DMV
  is enabled only for a pre-programmed time window during the pulse, generally between the X-point formation and the
  end of the post-heating phase." [S2] §3.3: "use of Disruption Mitigation when the plasma current is greater than 2 MA".
  [S7] Fig. 2 (p.2): one PETRA alarm of restart pulse #93912 is conditioned on "time > 0 s" and "plasma current >
  1.6 MA OR plasma energy > 5 MJ"; the user configures the conditioning of the eight alarms "within bounds" (p.4).
- **R-15** [S6], disruptions missed (2011–2012, 67 mitigated): 5 + 4 by configuration, 7 by late detection: "5
  disruptions were missed due to inhibits in the real-time protection systems that prevented the valve from firing.
  [...] 4 disruptions were missed due to an incorrect setting of the time window when the DMV was enabled. Finally, 7
  disruptions were detected at a plasma current lower than the minimum current needed for the DMV to be fired. In
  these cases, the first thermal quench event happened at higher current, but was not detected by any of the signals
  previously mentioned."

### Heating permissives (PEWS)

- **R-12** [S2]: "The Plant Enable Window System (PEWS) provides enable windows, realised as pulse trains over FO links,
  to heating systems plant based on time in the pulse and basic plasma conditions. [...] provided basic interlocks with
  plasma current and density along with the programmed time windows. Its successor, PEWS2 [...] provided enhanced neutral
  beam shine-through calculations on a PINI-by-PINI basis".

### Reliability: blind alarms, communication faults, watchdog

- **R-13** "Alarm systems such as the VTM define blind stop alarms to handle loss of critical signals during a shot. If
  RTPS detects communication or status faults with the alarm source systems, or real-time controllers, it can trigger
  the PTN system. A hardware watchdog signal to PTN ensures that RTPS is operational itself."

### Validation at the source (for the preprint narrative)

- [S3]: "much of the state machine logic implemented in RTPS GAMs was migrated to Level-1"; validation by "71 pulse
  schedules" defined by the Plasma Operations Group as "behavioural tests", plus "unit tests for each component".
  This is the gap the method fills: case-based tests → laws for every trace and every configuration. [S2]: an automatic
  post-pulse analysis produces "an ordered list of events that lead to termination of the pulse".

## 4. Hypothesis register (modelling decisions)

Each item is **ours**, not the source's. Columns: **Direction** = conservative (the model demands more than the system),
non-conservative (demands less), or neutral/fabricated. Since revision 4 a direction names the asset or hazard it refers to (wall heat loads, disruption loads, heating plant, coverage), because switching heating off or escalating to PTN protects some of them and endangers others; **If false** = which law loses its support. The laws that depend on
each hypothesis are in the table of `phase3-design.md` §2.

| # | Hypothesis | Direction | If false |
|---|---|---|---|
| **A-1** | **Order of the responses** `None < JTT < RTPS < PTN`. It orders **authority / irreversibility**, not safety. PTN at the top is forced by R-0 (latched output, fixed sequence): nothing softer can replace it. JTT vs RTPS is our choice: **it is not a JET concept** (Table 1 orders them per cell, not globally). Counter-hypothesis: during heating, escalating to PTN is what the RTPS exists to avoid ([S1]). [N1] confirms PTN on top ("a PIW stop can never preempt a PTN stop", PDF p.14). In [N1] "PIW" is the Protection of the ITER-like Wall project, and the PIW stops are the new shape-controller stop responses it introduced, triggered by the RTPS (PDF p.12); [N1] lists JTT among them (Fig. 6) and publishes no order between them. Identifying the PIW stops with the model's soft levels (JTT and RTPS) is ours. | neutral for the abstract laws (proved for both orders); not neutral for the certified instances, whose concrete layer runs `Ord1` only: there the order decides the Heating 2 both-hotspots cell (A-32) and whether a DHS read as JTT during an RTPS stop is answered (A-16) | P1 and P3 change meaning; C3 measures it |
| **A-2** | **Escalation = maximum.** On an alarm, `level := max(level, req)`: a soft request of equal or lower rank is ignored, while a PTN request always re-applies `to_ptn` and, with `dms` and `ip`, arms an idle DMS also when the PTN is already latched (A-11, A-20). Since revision 4, for an alarm that arrives during a stop `req` is the instance's secondary entry (A-3): the primary entry again in instances 1–3, PTN or nothing in instance 4 (A-35); the maximum is unchanged. R-6 says explicitly that JET can **suppress** an escalation ("allowing it to run to completion"), and [N1] describes another mechanism: "When the primary stop fails to mitigate the event [...] or the stopping strategy itself generates a higher priority termination event, then a secondary stop is started" (PDF p.14), from secondary stops configured with the pulse ("seven primary stops and six secondary", PDF p.13; "table" is our word, A-16). This is a **different policy**, not an approximation in a known direction; P1 and `stop_honoured` are theorems about our policy. The parts of them that concern the PTN and the primary stop are stated apart, each with its own basis: `p1a_ptn_latched` (R-0; [N1] PDF p.14); `d1b_primary_honoured` (R-5; [N1] PDF p.13); `d1a_ptn_honoured` ([N1] PDF p.13 for the PTN half; the same-step switch-off of both units is A-9); `piw_after_ptn_is_noop` ([N1] PDF p.13 calls such a request an "invalid task" for the shape controller; that it leaves the whole state and both counter commands unchanged is our formalisation, A-14); and `p1b_stop_never_cleared` (R-0 for the PTN latch; for soft stops our reading, A-14). The soft-over-soft part is ours. Its effect is **two-sided**: it demands at least the authority requested, but escalating a running soft landing to PTN is itself a disruption risk ("a sufficiently large and fast change in plasma parameters [...] can cause a loss of vertical position control", [IPB99-C3] p.2336), and it ignores a second request of equal or lower rank (a second PIW stop, or a DHS read as JTT during an RTPS stop: A-16). One consequence agrees with our reading of [N1]: RTPS "constrains to a maximum of two in sequence" (PDF p.13, in a paragraph about PIW stops); reading the cap as two soft (PIW) stops, a later PTN not counted, is ours, and under that reading the model complies by construction (two soft levels, strict order). It is not a separate law. | divergent from R-6 and [N1] (a different policy); two-sided: more authority is not more safety | P1 and `stop_honoured` no longer describe JET (`p1a_ptn_latched`, `d1b_primary_honoured` and the PTN half of `d1a_ptn_honoured` still do); "at least as severe" laws can reward an escalation that is physically harmful |
| **A-3** | **Primary/secondary: modelled in the concrete layer since revision 4** ([S1] R-6: "two levels of stop response, primary, and secondary"). An alarm that arrives while a stop is in force reads the instance's secondary table `sec_table(i, prog, t)` instead of the primary one; the abstract step still combines the request with the level in force by the maximum (A-2), so the abstract laws and the certificate are unchanged. Instances 1–3: `sec_table` = the masked primary entry, i.e. the reading of revisions 1–3 (our inference: A-16, A-24). Instance 4: an illustrative PTN-preferred table (A-35). JET's secondary table is not published ([N1] PDF p.13: "seven primary stops and six secondary"). The stop configuration is **not** frozen at the primary stop: both tables are read at the advancing programme phase (A-24, A-30). History: revision 1 dropped a `sec` field as unreachable (the only JTT cell is in `Heating2`, the JTT then wrote `Termination` into the phase, and that row has no RTPS). That reason became stale with F1 (A-24): the JTT no longer moves `prog`, so after a JTT in Heating 2 a later `Slow` or `Mchs` reads the Heating 2 row and escalates to RTPS, and an RTPS stop begun in Heating 1 keeps reading the table while `prog` advances (A-30). Second stops were routine at JET: 420 secondary stops, "of which 111 (22 %) were programmed to be a PIW stop" ([N1] PDF p.14). | — (a modelling decision; in instances 1–3 its consequence has the direction of A-16, in instance 4 that of A-35) | `stop_honoured` on concrete traces of instances 1–3 describes second alarms that JET resolves with another table; `sec_*` and `inst4_second_alarm_ptn` describe our parameter, not JET's content |
| **A-4** | **Columns absent from Table 1** (21 of the 56 cells; with the Blind row of A-25, 28 of the 56 are ours): *Fast* → PTN in every phase (mechanism supported by R-7: "fast termination [...] through PTN"; the uniformity across phases is ours); second MHD trigger = MHD; hotspot in both regions = the more urgent of MCHS/DHS according to A-1 (only non-trivial in Heating 1 → PTN and Heating 2 → RTPS; the concrete layer always uses `Ord1`, A-32). [N1] Fig. 6 lists a PIW "Fast" stop and a separately configured "MCHS+DHS" trigger: *Fast* → PTN everywhere is one configuration among several, and the maximum is a lower bound, not JET's rule (A-32). | fabricated | P11b, P13 |
| **A-5** | **Phases**: seven, in total order, as a configuration instance (R-1). `Advance` abstracts the programme clock ("Pulses are split into several timed 'phases'", [S3] p.5); a pulse that skips phases is modelled with consecutive `Advance`s with no events in between. Only `Advance` (and `Reset`) moves the programme phase `prog`; the JTT does **not** write it: it sets `jtt`, and the waveform phase is `wave = jtt ? Termination : prog` (A-24). Under PTN, and in Termination, `Advance` is the identity (R-0); under a soft stop it keeps moving `prog` (A-30). The heating enable window is `heat_win(phase) = phase ∈ {Heating1, Heating2}`, read on `wave`: it is **fixed in the model, the same for both units and for every instance**, not instance data (A-27). At JET the windows are programmed per pulse and heating in the current ramp-up is routine ("In JET, early LHCD is found to be the most efficient profile shaping method", [IPB99-C6] p.2516): changing that is a model change (a per-unit, per-instance window) and a recertification. **PEWS is another system**, independent and per PINI (R-12): folding it into the sequencer erases that independence (see A-17). | non-conservative in architecture and in coverage (no law is checked with heating outside Heating 1–2) | I1 loses its reading as PEWS; `heat_permissive`, `advance_units` speak of a window JET does not fix |
| **A-6** | **Two heating units** (NB, RF) for 16 PINIs + ICRF antennas. The **LH** klystrons, a third system with its own local manager in Fig. 1 of [S1], are not modelled, although [S1] gives them the same local rules ("Similar relationships and control rules can be determined for the RF antennae, and the LH klystrons", p.1295); applying local protection to RF is therefore [S1]'s, not ours. Each unit has **full, partial or no** power (`On`, `Reduced`, `Off`, plus `Ramping`): a local alarm takes out one PINI (one antenna for RF) and the unit goes to `Reduced` without ceasing to deliver power (R-9); compensation with other PINIs is not modelled. A reduced unit never goes **directly** back to `On` (F2d is a one-step law), but it can come back through `Off` (A-34). "Local" is a JET-scale property (one PINI of 16); at ITER scale "Termination of one beam will create a drop of about 50% in heating power" ([IPB07-C8] p.S400). Revision 2026-09-21 (F2); until then the model inhibited the whole unit. | conservative for wall heat loads in "no compensation" and "not directly back to On"; non-conservative through `Off` (A-34); neutral in the rest | P9, F2a, F2d |
| **A-7** | **Local protection = a reduction that is not undone.** R-8 speaks of "dynamically controlled limits [...] with fine granularity" (a limit that is recomputed, not a bit that sticks); an un-reduce after a hotspot is not published, so no event takes a `Reduced` unit back to `On` in one step. It is **not** "for the rest of the pulse": switching the unit off and on again restores full power (A-34). [S1] describes local protection as one "which inhibits individual heating components but allows the discharge to proceed" (p.1293), compatible with the PINI staying out. Hot spots are thermal and relax in seconds, so never un-reducing is unnecessary but safe for the wall; with less power the plasma can lose the H-mode, and at JET "the hysteresis is not observed" ([IPB99-C2] p.2197). Until 2026-09-21 the model inhibited the whole unit (a policy more restrictive than JET's and contrary to the concept of R-9): F2. | conservative with respect to wall heat loads; not with respect to confinement | F2d is superfluous; nothing is lost |
| **A-8** | **`plasma_ok`**: one boolean for the heating permissives (R-12). **Definition:** `plasma_ok` is the AND of every permissive of every unit: plasma current, the line density along every PINI's path (shine-through, which PEWS2 computes "on a PINI-by-PINI basis", and "both PEWS and PEWS2 were used in parallel", [S2]) and the edge density in front of every RF antenna. It merges a global interlock (Ip) with per-PINI and per-antenna, non-monotonic ones; it does not model validity or staleness (PDV falls through an ordered list of signals, [S2]). With that definition `False` switches both units off whenever any single permissive fails (conservative for heat loads, over-restrictive for availability) and `True` enables nothing a real permissive forbids. Under any weaker reading (e.g. "Ip and line-average density") `True` enables every PINI at once, which is non-conservative against PEWS2. A permissive that fails for some PINIs only (a physical `Reduced`) cannot be expressed. | conservative for heat loads with the AND definition; non-conservative against PEWS2 under any weaker reading | I1 says less than it seems |
| **A-9** | **Effect of the stops on heating.** Unit with four values `Off \| Ramping \| Reduced \| On` (A-26). On entering RTPS or JTT, every `On` or `Reduced` unit goes to `Ramping` (the references are overridden / the waveforms jumped to termination, which bring the power down: R-3, R-4; [S2]: "ramp down the plasma current and heating power") and `Ramping` never returns to `On`. `Ramping` means "coming down under a stop", to a lower level or to zero (R-3: 10 MW → 6 MW); every soft stop ramps **both** units. On entering Termination by `Advance` (natural end), the same. On reaching PTN by any path, `On`/`Reduced`/`Ramping` → `Off` in the same step: documented for the PTN outputs that trigger the DMS (R-11), otherwise only as R-0's "fixed shutdown sequence"; in the plant NBI takes 2 ms and RF about 38 ms ([S6], A-15). With a stop in progress, no new command turns anything on. **Revision 2 had "JTT de-energises"**: it contradicted R-4 (JTT *ramps*) and made the JTT harder than the RTPS stop against A-1. | conservative with respect to wall heat loads in "does not turn on"; neutral in "ramps" (physics does not order ramping against switching off: off is better for a hot tile and worse for disruption risk, since JET's edge density limit grows with input power, [IPB99-C3] p.2318) | I1, I2, P3, P4, P5 (its "no stop" clause), P6, D2 (every soft stop ramps both units), `stop_no_full_power`, `termination_no_full_power`; the switch-off on every path in P2, `ptn_no_heat` and `d1a_ptn_honoured`; that the heating stays off after the injection, in `dms_no_heat` |
| **A-10** | **Which stops arm the DMS is configuration**, not a property of the trigger ([S6]: "attached to any of the stops sent to the Plasma Termination Network (PTN) either directly or through an RTPS response"). The abstract model receives the `dms` bit with each stop; the concrete layer, **the same for every instance** (model code, not instance data), sets it for `Fast`, `MHD`, `MHD-B` within the window `dms_window(phase)` = X-point … Termination, read on the waveform phase, and to `False` for communication fault and watchdog (our decision; [S6] allows any). Two differences with JET: the window is set per pulse ("enabled only for a pre-programmed time window during the pulse", [S6] p.3; 4 missed disruptions came from a wrong setting, R-15), while the model fixes it; and [S6] closes it "generally between the X-point formation and the end of the post-heating phase", while the model keeps it open through the whole Termination row. Taking [S6]'s window literally is not a drop-in change: `dms_req` reads the waveform phase, so a window that closes before Termination would stop a DMS-marked alarm from arming after a JTT; the per-pulse values are not published (question Q7 in `SUPUESTOS_EVALUACION.md`). The real DMV triggers were the locked mode, the loop voltage and the current excursion ([S6] p.2 and Table 1), and from 2018 vertical displacements ([S7] p.4); the model has no column for them, and `Fast` is the only DMS-wired trigger that can stand for them (our reading). No source wires a hotspot to the valve. With the published Table 1 MHD never reaches the PTN: instance 2 (§6 of the design) does. | neutral in the wiring; non-conservative in coverage for the fixed window | P16/P17 are about the instance |
| **A-11** | **DMS sequence**: `Idle → Armed` on a PTN demand with `dms = True` and `ip` (A-22), also when the PTN is already latched (A-2), only from `Idle` (a repeated alarm does not re-arm or restart the wait; P15, P18); in `Armed` it waits for `HeatAck` or `ack_max` ticks; either → `Fired`, definitive until the end of the pulse (the valve "has to be refilled by an operator in the control room after every injection", [S6] p.2). The acknowledgement is an input not verified against the plant; **the timeout is the real path for RF** (R-11/[S6]: no acknowledgement signal, fixed 50 ms margin in 2012). By 2014–15 the trigger is passed on "When these systems are in a safe state", and the warning goes to "all auxiliary heating and diagnostic systems" ([K15] PDF p.6): the safe state covers diagnostics too, e.g. the Li-beam, which "takes 30ms to be turned off" ([S6] p.2). No source says what JET does if the acknowledgement never arrives. What is verified: arming and switching off occur in the same atomic step, and the wait is bounded in ticks. "conditioned with an acknowledgement" is **not** verified as a sequence (it would need commanded vs. reported state: future work). | conservative for the machine (a missing acknowledgement never blocks the injection, which must arrive before the thermal quench); non-conservative for the heating plant (it fires on timeout without plant evidence, possibly with RF still energised) | I4; the counter commands of `advance_frozen` and `piw_after_ptn_is_noop` |
| **A-12** | **Watchdog** from the PTN side: counter `hb` of ticks without `Heartbeat`; at `hb_max` the PTN fires (R-13). It runs from `Breakdown` (start of the pulse) with `hb = 0`; the guard is `level ≠ PTN`, not the phase. Whether the PTN by watchdog arms the DMS is configuration (A-10); in every instance it does not (`dms_on_watchdog = False`). A hard stop at high current can itself cause a disruption ([IPB99-C3] p.2336), and a disruption caused by a watchdog PTN is then **unmitigated**. No JET source says more than R-13 (question Q6 in `SUPUESTOS_EVALUACION.md`). | conservative with respect to loss of protection (the pulse does not continue unmonitored); a trade-off, not conservative, with respect to disruption loads (hard stop without DMS) | I6; the counter commands of `advance_frozen` and `piw_after_ptn_is_noop` |
| **A-13** | **Communication fault and blind alarms** (R-13) are two paths: `CommFault{dms, en}` → PTN from any state **if the check is enabled** (`en`, the instance's mask: A-21) and identity if not (F3); a blind alarm is the `Blind` trigger of the table (A-25). Until 2026-09-21 both were merged into an unconditional `CommFault`. [N1] confirms the path and its configurability: a PTN stop is triggered "If the communication between RTPS and SC is lost for more than a configurable number of control cycles (typically 3, i.e. 6 ms)" (PDF p.12), by either side ("SC (or RTPS) will automatically trigger"); the model merges both sides into one step (A-17). | neutral | P12, F3b |
| **A-14** | **End of pulse**: `Reset` is the *pulse on → post-pulse* transition of the supervisor of [S1]; accepted only if the pulse has ended (PTN active or Termination phase) and no mitigation sequence is armed; otherwise it is ignored. There is no published path that clears a PTN latch mid-pulse (R-10: enable/disable is pre-pulse; latch + time stamp; the post-pulse analysis reconstructs the cause). [N1] treats as an "invalid task" a request "for a PIW stop to be triggered after a PTN stop was already being executed" (PDF p.13), in the acceptance tests of the shape-controller simulator, which "still follows the correct path of action"; law `piw_after_ptn_is_noop` formalises it on the RTPS side as a no-op that leaves the whole state and both counter commands unchanged (our formalisation). No source publishes a way to cancel a soft stop either; [S1] presents stops as the way to end the pulse (overrides "which truncate the experiment and land the plasma safely", R-8): `p1b_stop_never_cleared` rests on that reading for soft stops, an argument from absence. | conservative | P20, `p1b_stop_never_cleared`, `piw_after_ptn_is_noop`; P1 loses content |
| **A-15** | **No real time or unbounded liveness.** Abstract ticks; it is not proven that the pulse terminates. That the watchdog latches the PTN in `hb_max` ticks and that an armed DMS fires in `ack_max` ticks is proven (`LAWS_JETPROT_LIVE.bend`, 2026-09-21), in ticks, not in ms. The missing numbers: VTM 10 ms [S4]; VTM→RTPS 100 Hz, RTPS 500 Hz [S1]; RTPS–SC communication loss after typically 3 cycles = 6 ms [N1]; PETRA hard real-time deadline 2 ms [S7]; alarm assertion times 0–20 ms, 14 ms for the current excursion ([S6] Table 1; [S7] shows a 20 ms example); NBI off 2 ms, RF off 38 ms, Li-beam off 30 ms, request→injection 50 ms, gas flight 3.4 ms [S6]; gas flight 1.1–4.8 ms depending on valve and gas (D2/Ar: DMV1 ~1.6/4.8 ms, DMV2 ~1.1/3.1 ms) [K15]. **I5 bounds a counter, not a latency.** | — | — |
| **A-16** | **No published secondary matrix.** Table 1 is "the primary stops table"; the secondary one and "not all possible combinations of control are allowable" (R-6) are not published. Since revision 4 the secondary response is a per-instance parameter of the concrete layer (A-3); its **content** stays ours: instances 1–3 read the primary table again, instance 4 uses an illustrative PTN table (A-35). What [N1] does publish: a PTN always pre-empts ("a PIW stop can never preempt a PTN stop", PDF p.14); RTPS "constrains to a maximum of two in sequence" (PDF p.13; read as two soft stops, which is our reading, the model's strict order respects this bound, A-2); JET configured "seven primary stops and six secondary" (PDF p.13); and "The preferred secondary plasma stop is the PTN slow stop" (Fig. 7 caption, PDF p.23): of 420 secondary stops, 111 were PIW (PDF p.14), so 309 were PTN, and Fig. 7 shows no JTT used as a secondary. Concrete consequence in instances 1–3, under A-1 and A-24: while an RTPS stop is in progress and the programme phase is Heating 2 (including an RTPS stop begun in Heating 1, A-30), a DHS or a both-hotspots alarm reads JTT or RTPS and is **ignored**; so is any second request of RTPS rank, since the model has one RTPS level (JET's "MHD Fast" PIW stop "is usually only used as a secondary stop strategy", Fig. 7 caption). On the Python reference model the ignored second requests reduce to 7 distinct cases per base instance: 6 are the same soft level requested again and 1 is a lower request (an RTPS stop, then a DHS read in the Heating 2 row). In instance 4 the same cases give PTN (`inst4_second_alarm_ptn`). When a secondary was started, the most used was the PTN slow stop (Fig. 7, a usage statistic); Fig. 7 counts only secondaries that were started, so it cannot weigh a PTN against letting the primary run to completion, which R-6 allows. Whether JET configured any secondary for a DHS during an RTPS stop is not published (questions Q2 and Q4 in `SUPUESTOS_EVALUACION.md`). | non-conservative in instances 1–3 with respect to JET's documented secondary practice (R-6 itself allows letting a stop run); instance 4: see A-35 | `stop_honoured` and `latched` describe, for instances 1–3, second alarms that JET resolves with the secondary table; H-G has a law of its own only for instance 4 (`inst4_second_alarm_ptn`) |
| **A-17** | **A single `step`** for the software path (RTPS) and the hardwired one (PTN, CISS). JET's three-layer architecture exists **for diversity**; the model says nothing about independence or common cause. It goes in the preprint summary, not here. At JET the layers are separate in hardware (the DMS power supplies "are triggered by the JET Pulse Termination Network (PTN) utilizing a direct fiber connection", [K15] PDF p.6), and diversity on the mitigation path shrank after 2018: PETRA "has been used as the sole disruption mitigation trigger" ([S7] p.4). | non-conservative in architecture | no law; the scope |
| **A-18** | **Total and well-formed alphabet**: no lost, duplicated, out-of-order or corrupted messages ([S1] has a message database with unique ids precisely for that); no alarm clearing (harmless with monotonic escalation). JET checks rather than assumes: each [S1] message id is "used to allow run-time verification that all systems on a virtual circuit are coherent" (p.1296), and each PETRA event detector also answers "has this event received enough data to be valid?" ([S7] p.2), with a blind alarm per PETRA alarm (A-25). | non-conservative | — |
| **A-19** | **Command = effect; no actuator model.** `nb`/`rf` are commanded state; the five actuator systems, the override waveforms and responses such as "transiting to a low-triangularity configuration" [S6] are outside the alphabet. | non-conservative | P2, P3 speak of the command |
| **A-20** | **A single PTN level.** The "slow/fast" output classes of the PTN (R-7) are represented only by whether the stop arms the DMS (A-10). [N1] Figs. 6–7 show five PTN (pre-PIW) stop classes on their axes (Slow, Fast, P1, Blind, Magnetic Emergency), and "any primary PTN stop can never be followed by a PIW secondary" (PDF p.14; law `sec_primary_ptn` for the secondary tables). With one level the model cannot tell JET's preferred secondary (PTN slow, A-16) from a PTN fast; a later DMS-marked stop under PTN still arms the DMS (rule 1). | neutral | — |
| **A-21** | **Bypass modelled only for the two reliability checks** of R-13 (communication fault, blind alarms), as the instance's mask (`Mask{comm, blind}`: instances 1, 2 and 4 with both enabled, instance 3 with both disabled and certified all the same). The PTN inputs and outputs (R-10) and the misconfigured window remain outside: exactly where JET lost 5 + 4 disruptions (R-15). It is the argument *in favour* of the method: certifying the configuration of each pulse. Revision 2026-09-21 (F3). | non-conservative in what is left out | F3b, `mask_*`, `blind_masked_no_response` |
| **A-22** | **DMV enabling condition as a Boolean input** (R-14 modelled on 2026-09-21; the earlier "threshold outside the alphabet" hypothesis is withdrawn). `ip` is state, input `Ip{ok}` like `Plasma{ok}`, and gates the **arming** of the DMS: without it no event arms (IP1). **Definition (revision 4):** `ip` is the verdict "plasma current above threshold **OR** plasma stored energy above threshold". In [S7] Fig. 2 (p.2) one PETRA alarm of restart pulse #93912 is conditioned on "time > 0 s" and "plasma current > 1.6 MA OR plasma energy > 5 MJ", and the user configures the conditioning of the eight alarms "within bounds chosen to guarantee its protection functions" (p.4): an example of the OR form, not PETRA's fixed rule; [K15] restricts operation without DMS to "plasma currents up to 2MA and total plasma energy (poloidal magnetic + kinetic) below 5MJ" (PDF p.5); [S6] (2011–12) used current alone (2.5 / 2.0 / 1.75 MA). **What changes:** only the meaning of the input: a low-current, high-energy plasma now counts as `ip = True`, which the current-only reading left unarmed (non-conservative). **What does not change:** the code and the laws (IP1–IP4, P16, P17 are statements about a Boolean); the numbers stay outside the model; the verdict is evaluated on arming and a later drop does not disarm (conservative with respect to [S6]/[S7]); and it gates only the arming: in PETRA an unmet condition makes the alarm "ignore its 'event occurring' inputs" ([S7] p.3), so neither stop nor MGI is issued, while the model still issues the PTN (conservative for the stop; question Q9 in `SUPUESTOS_EVALUACION.md`). 7 of the missed disruptions of R-15 are of this type (detected below the valve's minimum current, because the first thermal quench, at higher current, went undetected). | neutral (the laws are about a Boolean); conservative in "does not disarm" and in "the stop is still issued" | IP1–IP4, P16, P17 |
| **A-23** | **Configuration fixed during the pulse and equal to the certified instance** (configuration record: hashes of model, matrices, laws, checker version). Supported: "Each pulse has bespoke protection configurations, loaded into the run-time at pulse setup" ([S3] p.6). Caveat: [N1] reports that SC "was upgraded to provide up to four alternative sequences", each with its own stop settings, to be switched by RTPS in "some future developments" (PDF p.13); a plant using them would break this hypothesis. | — | everything |
| **A-24** | **Two views of time** ([S2] §3.4). `prog` is the Level-1 programme phase: the one that indexes Table 1 (and, since revision 4, the secondary table, A-3) and that only `Advance` (and `Reset`) moves. The JTT does not touch it: it sets `jtt`, and the waveform phase is `wave = jtt ? Termination : prog`. `wave` is read by: the heating permissive and I1, the DMV window, the end-of-pulse guard, I2/I3. `prog` is read by: the tables, `Advance`, D11/D12. Until 2026-09-21 the JTT wrote `Termination` into the single phase and a later alarm read the Termination row (F1). The two views are documented on the actuator side (the JTT fast-forwards the shape controller "to the time window corresponding to the pre-programmed termination time, ignoring any time windows in between", [N2] p.516), but **no source says which phase the RTPS reads for a new alarm during a stop**. What is published points elsewhere: responses depend on "pulse phase and state history" ([S3] p.7), and on the shape-controller side "The stop configuration will always be the one associated with the time-window where the primary stop was issued" ([N1] PDF p.13). The roles differ: the same page says "RTPS will select the stop and send it to SC"; the frozen "stop configuration" is stated for SC, and applying it to the RTPS table would be our inference. Against that frozen reading the model (where `prog` keeps advancing, A-30) is **two-sided**: weaker in 4 combinations (an RTPS stop begun in Heating 1 by `Slow` or `Mchs`, `prog` now Heating 2: a DHS or both-hotspots alarm reads JTT/RTPS and is ignored, where the Heating 1 row gives PTN) and stronger once `prog` reaches Termination (`Slow`, `Mchs`, `Dhs`, `BothHs` → PTN where the frozen row gives RTPS or nothing). Freezing alone was measured on the Python reference model and is **not adopted**: it removes more PTN escalations than it adds (the Termination-row cases are lost, the Heating 1 → Heating 2 DHS/both-hotspots cases are gained). That is evidence from the Python model, not a proof. Revision 4 keeps this reading and adds the secondary table instead (A-3, A-35); the open question is Q1 in `SUPUESTOS_EVALUACION.md`. Physically a table row stands for a plasma state, and during a stop the programme clock no longer describes the plasma. | not neutral: two-sided against [N1]'s frozen configuration (weaker in Heating 1 → Heating 2, stronger in Termination) | F1a–F1d, E3, D12 |
| **A-25** | **A blind alarm is a trigger of the table** (`Blind`: PTN row in every phase for instances 1, 2 and 4; no response when the mask disables it). R-13 says that the VTM defines "blind stop alarms"; its row is not published. Structural difference: in [N1] Figs. 6–7 "Blind" is a PTN (pre-PIW) stop class, not a column of the RTPS primary table, and in PETRA each alarm comes "with an accompanying blind alarm" ([S7] p.3): PTN is the documented response, the column is our encoding. | fabricated (the row) | `asm_*_Blind`, `blind_masked_no_response` |
| **A-26** | **No `Inhibited`.** With local protection reducing (A-7), no pulse event produced the inhibited state; a unit disabled before the pulse (R-10) is configuration, not an event. The value was removed and with it P8 (`inhibit_latched`) and E1 (`inhibit_source`), which would have been left vacuous (gate C2). | — | — |
| **A-27** | **Heating window fixed and shared** (implicit until revision 4). `heat_win(phase) = phase ∈ {Heating1, Heating2}`, read on `wave`, is the same for NB and RF and for every instance; it is model code, not instance data. At JET PEWS "provided basic interlocks with plasma current and density along with the programmed time windows" ([S2]) and "Each pulse has bespoke protection configurations" ([S3] p.6). Heating in the current ramp-up is routine ("In JET, early LHCD is found to be the most efficient profile shaping method", [IPB99-C6] p.2516; "Most of the experimental scenarios use a substantial amount of heating and/or current drive in the current ramp-up phase", [IPB07-C6] p.S287), and each system needs its own window (LH early, at low density; NB line density per PINI and current; ICRF edge density at the antenna). | non-conservative in coverage: no law is checked in a state with heating before Heating 1 or with per-unit windows | I1, `heat_permissive`, `advance_units`, `heat_frame` hold for this window only; a per-unit, per-instance window is a model change and a recertification |
| **A-28** | **Loss of plasma conditions switches both units off in the same step** (implicit until revision 4). `Plasma{False}` takes `On`, `Reduced` and `Ramping` to `Off` for NB and RF alike, also under a soft stop; once `plasma_ok` returns, a new `HeatOn` can switch a unit on again if no stop is in progress. No JET source describes the behaviour on a loss of the permissive. Switching NB off at once is right (shine-through onto the wall); for RF the relevant condition is edge density at the antenna (A-8). A brief dip (an ELM) that switches everything off can itself push the plasma out of H-mode (no L–H hysteresis at JET, [IPB99-C2] p.2197). | conservative with respect to wall heat loads only; not with respect to confinement | I1 forces it (a unit at power needs `plasma_ok`); `f4_units_frame` exempts it |
| **A-29** | **Leaving the window switches off; entering Termination ramps** (implicit until revision 4). `Advance` into a phase outside `heat_win` takes every unit to `Off`; `Advance` into Termination takes `On`/`Reduced` to `Ramping`, and this case takes precedence over the window rule. With the fixed window (A-27) only the second case is reachable. It matches a natural termination, where heating ramps with the current (a stable ramp-down needs "a wide density window of 0.2–1.0nGW", [IPB99-C8] p.2614); no JET source describes it. | neutral | `advance_units`, `advance_to_termination_ramps` (derived), `termination_no_full_power` |
| **A-30** | **The programme phase keeps advancing during a soft stop** (implicit until revision 4). `Advance` is blocked only under PTN and in Termination, so under an RTPS or JTT stop `prog` moves on and later alarms read the new row (A-24). On the shape-controller side JET does the opposite: pre-PIW, "Once the execution of a stop has started the timewindows are ignored until the end of the experiment" ([N1] PDF p.12); for PIW stops the configuration is "the one associated with the time-window where the primary stop was issued" (PDF p.13). No source describes the RTPS itself. Not changed in revision 4 (see A-24). | two-sided against the frozen reading (see A-24) | `phase_monotone`, `advance_frozen`, `advance_is_one_step` still hold; the concrete response to a second alarm changes (`stop_honoured` on concrete traces) |
| **A-31** | **The "Slow" column is the generic slow-termination trigger of R-2** (stated in R-5 as our inference). The header "Phase Slow MHD MCHS DHS" is printed in [S1] (p.1295) and [N1] Fig. 6 lists a PIW stop named "Slow"; neither source states the identification. | fabricated (the identification; the 7 cells are published) | the 7 `pub_*_Slow` laws are JET fact plus our inference |
| **A-32** | **"Both hotspots" is the more urgent of MCHS and DHS** (part of A-4). The concrete layer always takes the maximum under `Ord1` (`lvl_max1`), whatever order the abstract laws use. At JET "MCHS+DHS" is a trigger configured on its own ([N1] Fig. 6), so its row is not derived from the other two. Simultaneous main-chamber and divertor hotspots point to a global cause (too much power reaching the edge), so the maximum is a reasonable **lower bound**; in Heating 2 it gives RTPS over JTT only because of A-1. | fabricated (a lower bound that depends on A-1) | `asm_*_BothHs` (7 cells) |
| **A-33** | **A second local alarm has no effect** (implicit until revision 4). `reduce` maps only `On` to `Reduced`, so a unit carries at most one "PINI out" whatever the number of local alarms. [S1] takes a PINI out per alarm ("the relevant PINI should be turned off to prevent further overheating", p.1295), so a second alarm on another footprint should reduce the power further. | non-conservative for the local protection layer (one protective action fewer than JET), mitigated only if the same condition later raises a global alarm (R-8) | `f2a_local_reduces`, `local_is_local` describe a single reduction level; counting reductions is a model change (question Q5 in `SUPUESTOS_EVALUACION.md`) |
| **A-34** | **A local reduction is forgotten through `Off`** (found in revision 4 by running the reference model). `f2d_reduced_never_returns` forbids `Reduced → On` in one step only. `HeatOff{u}` or `Plasma{False}` takes a `Reduced` unit to `Off`, and a later `HeatOn{u}` (inside the window, with `plasma_ok`, no stop in progress) returns it to `On`, with the PINI that R-9 took out back in. Reproduced in `pymodel/jetprot_ref.py`, instance 1: Heating 1, `HeatOn{Nb}`, `Local{Nb}`, `HeatOff{Nb}`, `HeatOn{Nb}` → `On`; also with `Plasma{False}`, `Plasma{True}` in place of `HeatOff`. No JET source says whether the PINI stays out after such a cycle (question Q5). | non-conservative for the local protection layer; A-6, A-7 and SR-9 hold step by step, not over the pulse | a trace-level "reduced never returns in the pulse" law would fail; F2d is one-step |
| **A-35** | **Illustrative secondary table (instance 4)** (revision 4). `sec_table(Inst4, p, t)` = PTN wherever the masked primary entry is not None, None elsewhere; instance 4 otherwise equals instance 1 (Table 1 + A-4, both checks on). Basis: the [N1] Fig. 7 caption, "The preferred secondary plasma stop is the PTN slow stop" (PDF p.23), which is a usage statistic of the configured secondaries, not a configuration rule, and is partly forced by "any primary PTN stop can never be followed by a PIW secondary" (PDF p.14). Declared consequences: (i) a repeated alarm counts as a failed primary and escalates to PTN, because the model has no time and cannot tell a new alarm from one still asserted (question Q4); (ii) PIW secondaries, which JET did configure (111 of 420, and the exception of the "PIW secondary related to MHD driven stops", PDF p.14), cannot be expressed; (iii) the DMS bit is still `dms_req`, so a PTN secondary for `Slow`, `Mchs`, `Dhs` or `BothHs` (the triggers whose primary entry is a soft stop in some phase) does not arm the DMS and a disruption it provokes is unmitigated ([IPB99-C3] p.2336); (iv) under a stop every alarm asks for PTN or nothing, so a planted de-escalation defect cannot show in this instance (the differential stage declares it unobservable). It is **not** JET's table, which is unpublished. | fabricated content on a documented mechanism: more authority, not necessarily more safety | `sec_inst4`, `inst4_second_alarm_ptn` and the instance-4 cells of `concretize_is_the_table` describe our illustrative table, not JET's |

## 5. Size of the finite domain (≈10⁵ cells criterion)

Finite control: 7 phases × 4 levels × 3 DMS states × 2 (`plasma_ok`) × 4² units = **2 688 states**. **Abstract**
alphabet (the configuration enters as event payload, see `phase3-design.md` §1): `Advance`, `Stop{req, dms}` ×8,
`Local{u}` ×2, `HeatOn{u}` ×2, `HeatOff{u}` ×2, `Plasma{ok}` ×2, `CommFault{dms}` ×2, `Heartbeat`, `Tick{dms}` ×2,
`HeatAck`, `Reset` = **24 variants**. The verdicts `bh`, `bt` enter only the `Tick` arm: columns = 22 + 2 × 4 = 30
→ **80 640 cells** (full product 2 688 × 24 × 4 = 258 048; Phase 2b: 32 256 in 34 s). Estimate: ≈ 90 s per
check; re-evaluated in every file that imports it: gate ≈ 6–8 min.

**Revision 2026-09-21 (blocker 4):** 7 programme phases × 2 (`jtt`) × 4 levels × 3 DMS × 2 (`plasma_ok`) × 2 (`ip`) × 4²
units = **10 752 states**; abstract alphabet of **28 variants** (`Ip{ok}` ×2, `CommFault{dms, en}` ×4); columns
23 + 5 × 4 = 43 → **462 336 cells per order**. Certificate at runtime (JS): 27 s for both orders; concrete layer 3
instances × 7 × 2 × 24 plant events = 1 008 cells. (Until the audit of 2026-09-22 this line said 22 events and 924
cells, a miscount: the enumerator of that run already covered all 24 concrete events, `XIp{ok}` included.)

**Revision 4 (2026-09-22):** the abstract domain is unchanged (10 752 states, 43 columns, 462 336 cells per order). The
concrete layer has 4 instances and a level axis (the level in force selects the primary or the secondary table): 4 × 7
× 2 × 4 × 24 = **5 376 cells** (`check_concretize`). The concrete alphabet `CEv` has 24 events: `XAdvance`,
`XAlarm{t}` ×8, `XLocal{u}` ×2, `XHeatOn{u}` ×2, `XHeatOff{u}` ×2, `XPlasma{ok}` ×2, `XIp{ok}` ×2, `XCommFault`,
`XHeartbeat`, `XTick`, `XHeatAck`, `XReset`.

Spurious states: for the *preservation* laws the hypothesis `inv(s)` is false and the implication holds; the *step*
laws do not carry that hypothesis and **must also hold in those states** (that is how they are stated). Verified mechanically
in Python over the full domain before writing Bend (`phase3-design.md` §9, H13).
