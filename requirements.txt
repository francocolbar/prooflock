# Python side of the gates: py -3.14 -m pip install -r requirements.txt
# Pinned to the versions the committed v3/results.json / v3/recheck.json were produced with
# (see their provenance block).
hypothesis==6.168.0
# only for heat/ and v2/heat/ (the JAX exercises); v3/ does not need them
numpy==2.4.3
jax==0.11.2
