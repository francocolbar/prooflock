# generated outputs and caches
__pycache__/
*.pyc
.hypothesis/
env/hello/hello_kernel_*.py
env/check_env.log
v3/*.log

# gate outputs of the exercises (heat/, v2/) are regenerated by their run.py and stay out;
# v3/results.json, v3/recheck.json and SHA256SUMS are COMMITTED: they are the reference run
# (`py -3.14 v3/run.py all --full`) with its provenance block; re-run and compare.
results.json
recheck.json
!v3/results.json
!v3/recheck.json
